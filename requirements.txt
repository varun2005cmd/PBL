# =============================================================================
# Smart Door Lock – requirements.txt
# Python 3.11.9 | Raspberry Pi 5 (ARM64 / aarch64)
# =============================================================================

# ── Web framework ─────────────────────────────────────────────────────────────
Flask==3.0.3
Flask-Cors==4.0.1
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.6
SQLAlchemy==2.0.36
gunicorn==21.2.0
requests==2.32.3

# ── Scientific / image ────────────────────────────────────────────────────────
numpy==1.26.4
Pillow==10.4.0


# ── ML / AI ───────────────────────────────────────────────────────────────────
scikit-learn==1.3.2
facenet-pytorch==2.5.3

# PyTorch CPU-only for ARM64 (Pi 5).
# Pinned versions known to ship aarch64 wheels without NVIDIA bloat:
torch==2.2.2
torchvision==0.17.2

# mediapipe 0.10.14 has a Linux aarch64 / Python 3.11 wheel on PyPI.
mediapipe==0.10.14

# ── Hardware (Pi 5) ───────────────────────────────────────────────────────────
smbus2==0.4.3
# CRITICAL FIX: Use rpi-lgpio instead of RPi.GPIO for Raspberry Pi 5 (RP1 chip)
rpi-lgpio==0.6
