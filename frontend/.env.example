# Copy this file to .env and set the Pi's IP address.
# Required when developing on a machine that is NOT the Pi itself.
# Leave commented out if the React app is served directly from the Pi.

# REACT_APP_API_BASE=http://192.168.1.42:5000
