# Quickstart

```bash
cd frontend
npm install
npm start
```

The dashboard expects the Flask backend on port `5000`.

For a Pi-hosted dashboard, open:

```text
http://<pi-ip>:3000
```

For a production static build:

```bash
npm run build
```
