# =============================================================================
# backend/requirements.txt
# Install with: pip install -r requirements.txt
#
# All packages are chosen for CPU-only operation (Raspberry Pi compatible).
# =============================================================================

# ---- Web framework ----
flask>=3.0.0
flask-cors>=4.0.0
flask-sqlalchemy>=3.1.0

# ---- Face detection (MTCNN) ----
# mtcnn uses TensorFlow internally; tf-lite-runtime is an option on RPi
mtcnn>=0.1.1
tensorflow-cpu>=2.14.0       # CPU-only build; use tflite-runtime on RPi

# ---- Face embedding (FaceNet via facenet-pytorch) ----
facenet-pytorch>=2.5.3
torch>=2.1.0                 # CPU-only; install separately on RPi:
                             #   pip install torch --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.16.0
Pillow>=10.0.0

# ---- Head pose / liveness (OpenCV) ----
opencv-python-headless>=4.8.0  # headless = no GUI dependency (RPi/server safe)
numpy>=1.24.0

# ---- Recognition (SVM) ----
scikit-learn>=1.3.0

# ---- Evaluation / visualisation ----
matplotlib>=3.8.0
seaborn>=0.13.0              # optional — nicer default themes for eval plots

# ---- Utilities ----
gunicorn>=21.0.0             # production WSGI server
