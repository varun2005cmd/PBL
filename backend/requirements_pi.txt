# =============================================================================
# backend/requirements_pi.txt
# Raspberry Pi 5  (aarch64, Python 3.11 recommended)
#
# Install order matters -- read the notes below before running pip install.
#
# STEP 1: Install PyTorch FIRST (Pi aarch64 is NOT on PyPI main index):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# STEP 2: Install system deps (apt):
#   sudo apt install -y libhdf5-dev libatlas-base-dev libjpeg-dev libopenblas-dev
#
# STEP 3: Install this file:
#   pip install -r requirements_pi.txt
# =============================================================================

# ---- Web framework ----
flask>=3.0.0
flask-cors>=4.0.0
flask-sqlalchemy>=3.1.0
Werkzeug>=3.0.0
gunicorn>=21.0.0

# ---- Face detection (MediaPipe official aarch64 wheels) ----
mediapipe>=0.10.0

# ---- Face embedding (FaceNet) ----
# NOTE: torch + torchvision must already be installed via the URL above.
facenet-pytorch>=2.5.3
Pillow>=10.0.0

# ---- OpenCV (headless = no GUI / no libGL required) ----
opencv-python-headless>=4.8.0

# ---- Numerical / ML ----
numpy>=1.24.0
scikit-learn>=1.3.0

# ---- Hardware (Raspberry Pi OS ships RPi.GPIO; uncomment if missing) ----
# RPi.GPIO>=0.7.1
smbus2>=0.4.3

# ---- Misc ----
requests>=2.28.0
